Types must be first class objects.

object type.  # returns the type of the given object

Type type.    # returns self

Type is the only object who's type is itself

if and only if object type = Type then "object" is the Type object.

So this is true:

Type type = Type.
